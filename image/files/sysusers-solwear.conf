# The unprivileged account the daemon and the kiosk run as. systemd-sysusers
# creates it on first boot, so the image needs no chroot at build time.
u solwear - "SolWear system" /var/lib/solwear /usr/sbin/nologin
m solwear video
m solwear render
m solwear input
m solwear netdev
