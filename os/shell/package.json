{
  "name": "@solwear/shell",
  "version": "0.1.0",
  "private": true,
  "description": "SolWear OS system shell: watchface host, launcher, notifications, settings, wallet confirmation",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "esbuild": "0.24.0",
    "typescript": "5.6.3"
  },
  "engines": {
    "node": ">=22"
  }
}
